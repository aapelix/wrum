{
	"name": "game",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "bun run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3001.0.19"
	},
	"devDependencies": {
	  "@types/bun": "latest",
		"vite": "^8.0.1"
	},
	"peerDependencies": {
    "typescript": "^5"
  }
}
